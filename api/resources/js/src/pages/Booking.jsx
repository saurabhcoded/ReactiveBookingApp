import React from "react";
import { Stack, Avatar, Container, Typography } from "@mui/material";

const Booking = () => {
    return (
        <Container sx={{ padding: "40px 10px" }}>
            <Typography textAlign={"center"} component={'h1'} variant={'h4'}>
                This is the Reactive Booking App
            </Typography>
        </Container>
    );
};

export default Booking;
